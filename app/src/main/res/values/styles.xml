<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="StartTheme" parent="AppTheme.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/bg_app_splash</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorControlNormal">@color/black_a054</item>
        <item name="colorAccent">?android:attr/textColorPrimary</item>
    </style>

    <style name="LightIconButton" parent="@style/Widget.AppCompat.ImageButton">
        <item name="android:tint">@color/gray_600</item>
        <item name="android:background">@drawable/bg_transparent_pressed</item>
    </style>

    <style name="LightIconView">
        <item name="android:tint">@color/gray_600</item>
        <item name="android:scaleType">center</item>
        <item name="tools:ignore">ContentDescription</item>
    </style>

    <style name="LightIconText">
        <item name="android:textColor">@color/gray_600</item>
    </style>

    <style name="AppTheme.RatingBarIndicator">
        <item name="colorControlNormal">@color/secondary_dark</item>
        <item name="colorControlActivated">@color/secondary</item>
    </style>

</resources>